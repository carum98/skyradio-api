networks:
  skyradio-network:
    external: true

services:
    api:
      environment:
        - NODE_ENV=production
      networks:
        - skyradio-network
    
    db:
      volumes:
        - ./.db:/var/lib/mysql
      networks:
        - skyradio-network
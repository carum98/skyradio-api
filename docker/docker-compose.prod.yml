networks:
  skyradio-network:
    external: true

services:
    api:
      environment:
        - NODE_ENV=production
      networks:
        - skyradio-network
    
    db:
      ports:
        - 3306:3306
      volumes:
        - ./.db:/var/lib/mysql
      networks:
        - skyradio-network